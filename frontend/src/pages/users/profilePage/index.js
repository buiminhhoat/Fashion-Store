import {memo} from "react";

const ProfilePage = () => {
    const divStyle = {
        margin: "100px",
    };

    return (
        <div style={divStyle}>profile</div>
    );
}

export default memo(ProfilePage);