import {memo} from "react";

const HomePage = () => {
    const divStyle = {
        // margin: "100px",
    };

    return (
        <div style={divStyle}>hompage</div>
    );
}

export default memo(HomePage);