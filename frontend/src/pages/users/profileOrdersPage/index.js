import {memo} from "react";

const ProfileOrdersPage = () => {
    const divStyle = {
    };

    return (
        <div style={divStyle}>Profile Orders</div>
    );
}

export default memo(ProfileOrdersPage);